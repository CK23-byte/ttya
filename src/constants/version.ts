/**
 * Application version
 *
 * This version matches the version in package.json and is used throughout
 * the app to display the current version to users.
 *
 * Update this when bumping the version in package.json
 */
export const APP_VERSION = '2.13.0'
export const APP_NAME = 'TalkToYouAI'
export const BUILD_DATE = new Date().toISOString().split('T')[0] // YYYY-MM-DD
